기록업데이트
에러->수정어려움
=>다시 돌아가기가능

깃
: 코드변경점 기록(버전관리도구)


깃허브
: 백업 공유가능(온라인코드저장소)
->협업가능

pwd : 작업중 폴더 위치
~ : home : 상위폴더
ls 리스트 : 폴더내역, 파일내역
ls -a : 리스트 올
cd : 체인지디렉토리
cd ../바깥으로 이동
cd/desktop/git-practice

git init
: 초기 세팅 딱 한번만

git add파일지정
git commit -m : 메세지 작성

git status

git log : 
git diff 코드 변경확인

git reset : 과거로 돌아가기

git push 

git branch

git switch 

git checkout 브랜치이름

git switch -c브렌치이름

git checkout -b